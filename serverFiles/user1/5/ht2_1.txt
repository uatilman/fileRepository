// Александр, добрый день!
//
//        Проект залил на гитхаб https://github.com/uatilman/fileRepository.git
//
//        Что сделано:
//        1. Небольшой клиент на JavaFX, в котором работает авторизация, есть окно с логом - рудиент от чата, далее сделаю всплывающие окна.
//        2. При отправки любого текста пересылается жестко заданный файл.
//        3. Сервер сделан многопользовательский.
//		  4. Сообщения сделаны отдельным классом, тип сообщения enum.

//Вопросы и проблемы:
//1. Покажите пожалуйста как добавлять maven к javaFX, чтобы все неломалось. Я сделал, но не понял как и боюс повторят, чтоб все несломалось.
//2. Не могу справиться с созданием директории после получения директории.
//3. Во второй методичке непонятна логика 
if (files.contains(requestFile)){
files.remove(requestFile);
}
//зачем для удаления файла необходимо его сначало получить файл от клиента.
//4. Для сохранения файла на сервере я использовал запись потока в буфер:
    public void writeFile(File file) {
        try (FileInputStream fin = new FileInputStream(file);
             FileOutputStream fos = new FileOutputStream("src\\test\\java\\server\\" + file.getName())) {
            byte[] buffer = new byte[fin.available()];
            fin.read(buffer, 0, buffer.length);
            fos.write(buffer, 0, buffer.length);
            System.out.println("Закончил писать файл: " + file.getName());
        } catch (IOException ex) {
            System.out.println(ex.getMessage());
        }
    }
//5. Хочу сделать синхронизация файлов, по принципу, что в клиенте указыается с какой папки начинать синхронизацию, и на сервере появляется копия. При дальнейшихподключениях клиент сравнивает деревья (для них хочу сделать отдельный класс) на сервере и на компе, например по дате изменения (или возможно следует сравнивать по хешу?), и запрашивает, отправляет, удаляет файлы у ембя и на сервере.
//Как вы думаете, это нормальная идея?
	